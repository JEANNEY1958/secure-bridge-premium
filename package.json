{
  "name": "secure-bridge-premium",
  "version": "1.0.0",
  "description": "Secure Bridge Premium - Local API key storage and trade execution for Xchange Suite ETF100",
  "main": "dist/main/index.js",
  "author": "Xchange Suite",
  "license": "MIT",
  "scripts": {
    "start": "electron .",
    "build": "tsc && npm run copy-renderer",
    "copy-renderer": "node scripts/copy-renderer.js",
    "dev": "npm run build && electron .",
    "dist:win": "npm run build && node scripts/build-win.js",
    "dist:mac": "npm run build && electron-packager . \"Secure Bridge Premium\" --platform=darwin --arch=x64 --out=release --overwrite --ignore=\"node_modules/(electron-packager|electron-winstaller)\" --ignore=\"\\.git\" --ignore=\"release\"",
    "dist:linux": "npm run build && electron-packager . \"Secure Bridge Premium\" --platform=linux --arch=x64 --out=release --overwrite --ignore=\"node_modules/(electron-packager|electron-winstaller)\" --ignore=\"\\.git\" --ignore=\"release\""
  },
  "dependencies": {
    "@alpacahq/alpaca-trade-api": "^3.0.0",
    "electron-store": "^8.1.0",
    "node-machine-id": "^1.1.12",
    "ws": "^8.14.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/ws": "^8.5.0",
    "archiver": "^6.0.1",
    "electron": "^28.0.0",
    "electron-packager": "^17.1.2",
    "electron-winstaller": "^5.3.1",
    "typescript": "^5.0.0"
  }
}
